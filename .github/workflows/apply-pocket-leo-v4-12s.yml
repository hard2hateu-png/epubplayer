name: Apply Pocket Leo v4 12s

on:
  push:
    branches:
      - fix/pocket-leo-v4-12s

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/pocket-leo-v4-12s
          fetch-depth: 0

      - name: Apply Leo v4 patch
        run: python .github/scripts/apply_pocket_leo_v4_12s.py

      - name: Check diff
        run: git diff --check

      - name: Build
        run: |
          npm ci
          npm run build

      - name: Commit patched sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/tts/pocketService.ts src/features/player/TTSBufferManager.ts
          if git diff --cached --quiet; then
            echo "No source changes to commit"
            exit 0
          fi
          git commit -m "Use one memory-safe 12-second Leo conditioning window"
          git push origin HEAD:fix/pocket-leo-v4-12s
