name: Apply Pocket Leo identity v3

on:
  push:
    branches:
      - fix/pocket-leo-identity-v3

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/pocket-leo-identity-v3
          fetch-depth: 0

      - name: Apply Leo identity patch
        run: python .github/scripts/apply_pocket_leo_identity_v3.py

      - name: Check diff
        run: git diff --check

      - name: Build
        run: |
          npm ci
          npm run build

      - name: Commit patched sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/tts/pocketService.ts src/features/player/TTSBufferManager.ts
          if git diff --cached --quiet; then
            echo "No source changes to commit"
            exit 0
          fi
          git commit -m "Rebuild Leo from one continuous 30-second reference"
          git push origin HEAD:fix/pocket-leo-identity-v3
